export async function importFile(importPath: string) {
    // return import(importPath);
    const awsConfig = {
        region: '',
        accessKeyId: '',
        secretAccessKey: '',
    };
    return awsConfig;
}
